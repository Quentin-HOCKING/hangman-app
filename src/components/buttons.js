function ValidateWordButton(props) {
  return (
    <button onClick={props.onClick}>
      Login
    </button>
  );
}

function ValidateLetterButton(props) {
  return (
    <button onClick={props.onClick}>
      Logout
    </button>
  );
}
